import React from 'react';

const PorfolioHeader = () => {
    return (
        <div className = "Portfolios__header">
            <h2>An array of live websites and demos</h2>
        </div>
    );
};

export default PorfolioHeader;